class AreaCalculator {
    public static void main(String[] args) {
        var scanner = new java.util.scanner(System.in);
        scanner.useLocale(java.util.Locale.ENGLISH);
        var inputline = s.nextline();

        s.close();
    }
}